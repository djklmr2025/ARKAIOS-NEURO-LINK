🧹 Remove Unused `path` Import in `main.js`

🎯 **What:** Removed the unused `path` module import from `main.js`.
💡 **Why:** The `path` module was required but never used in the file. Removing it reduces dead code and improves code cleanliness and maintainability.
✅ **Verification:** Verified there are no usages of the `path` module in `main.js` using `grep`. Syntax checked using `node -c main.js` and confirmed there are no regressions using `node --test`.
✨ **Result:** A cleaner `main.js` file with no unused imports.
